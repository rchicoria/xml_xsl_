Feito:

- Complicar a estrutura do XML (atributos, mais nós)


Para fazer:

- Site offline e usar ficheiros em vez de HttpClient
- Ir buscar lista a várias categorias e não à das novidades

- Schema
- XSL
- HTML
